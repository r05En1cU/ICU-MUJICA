# ICU-MUJICA v2.0.0 Phase 1 Brainstorm

> 日期：2026-06-04  
> 阶段：目录结构与工程边界初步梳理  
> 状态：brainstorming，不作为最终架构定稿

## 背景

当前 v2 方向从 v1 的多 Docker、多 Agent 设计，收敛到单个 LLM 配合 Harness + Skill 的方式，通过明确的 skill 定义、模块化 workflow、多轮对话运行机制，实现前端设计、仿真与后端流程自动化。

README 中现有计划目录大致如下：

```text
ICU-MUJICA
|- src
|  |- common
|  |- skill
|  |  |- graph
|  |  |- evaluate
|  |  |- execute
|  |- workflow
|- workspace
|- output
|  |- frontend
|  |- backend
|- docker_compose.yml
|- requirements.txt
|- .env
```

这个结构已经捕捉到了 v2 的核心概念，但目前仍然偏概念草图。主要问题不是“目录不够多”，而是几个抽象轴还没有完全拆开：skill 是能力单元，workflow 是编排方式，evaluate/execute 是运行范式，workspace/output 是运行时产物，common 是协议与共享基础设施。

## 当前目录结构的主要问题

### 1. `src/skill` 与 `src/workflow` 的边界不够清楚

当前 `skill` 下存在 `graph`、`evaluate`、`execute`，同时还有单独的 `workflow` 目录。

这会导致后续实现时出现归属问题：

- 一个具体 skill 的 evaluate 逻辑应该放在 `skill/evaluate`，还是放在该 skill 自己的目录下？
- execute 是所有 skill 的统一运行器，还是每个 skill 自带的执行阶段？
- workflow 是调用 skill 的编排代码，还是一类可配置的流程定义？
- SkillGraph 属于 skill 定义、运行时状态，还是 workflow 编排的一部分？

建议尽早明确：

- `skill`：描述一个能力单元能做什么、需要什么输入、产生什么输出。
- `workflow`：描述多个能力单元如何被编排。
- `evaluate/execute`：更像运行模式或协议阶段，不一定适合作为顶层 skill 子目录。
- `graph`：如果表示 skill 依赖关系，可能更接近 registry / planner / workflow graph，而不是普通 skill。

### 2. `common` 容易演化成万能目录

README 中 `common` 目前承担共享类型、skill 统一化协议模型、pydantic 检查类型等职责。

早期这样写很方便，但项目一旦增长，`common` 容易变成所有模块都依赖的“杂物箱”，从而造成循环依赖和边界模糊。

建议将 `common` 拆成更明确的职责目录，例如：

- `schemas/`：Pydantic 类型、输入输出模型、协议数据结构。
- `protocols/`：skill 运行协议、消息结构、阶段定义。
- `runtime/`：执行上下文、状态管理、artifact 管理。
- `utils/`：真正无业务语义的小工具函数。

如果 phase 1 想保持简洁，也至少建议避免把所有共享逻辑都默认放进 `common`。

### 3. `workspace` 和 `output` 更像运行时目录，而不是源码目录

`workspace` 是智能体 active 态下的工作空间，`output` 是生成产物目录。这两类目录通常会包含临时文件、运行结果、测试产物、大文件或模型生成内容。

建议明确它们的 git 策略：

- 是否提交目录本身？
- 是否忽略目录内大部分生成文件？
- 是否需要 `.gitkeep` 或 README 说明用途？
- 是否需要按 run id / session id / task id 分层？

建议初期使用：

```text
workspace/
artifacts/
```

并通过 `.gitignore` 忽略其中生成内容，只保留必要说明文件。

### 4. `frontend/backend` 命名可能和 IC / EDA 语境不完全贴合

README 中 `output/frontend` 对应“前端输出（架构图、RTL等）”，`output/backend` 对应“后端输出（报告、网表等）”。

如果项目目标是 IC 自动化，`frontend/backend` 虽然可以对应芯片设计流程里的前端/后端，但也容易被软件工程语境误读成 Web 前端 / 服务端后端。

可以考虑更贴近产物性质的命名：

```text
artifacts/
|- design/          # 架构、规格、RTL
|- verification/    # testbench、仿真结果、覆盖率
|- synthesis/       # 综合脚本、网表、时序报告
|- reports/         # 总结报告、评估报告
```

如果保留 `frontend/backend`，建议在文档里明确这里指的是 IC design flow，而不是 Web application 的前后端。

### 5. 缺少测试、文档、样例和配置目录

当前计划结构缺少几个对后续迭代很关键的支点：

- `tests/`：验证 skill 协议、workflow 编排、schema 校验、RAG 数据处理。
- `docs/`：记录架构决策、运行模式、skill 规范、目录约定。
- `examples/`：保存最小可运行案例、golden cases、参考输入输出。
- `configs/`：保存模型配置、RAG 配置、workflow 配置、路径配置。

由于 v2 的目标包含自定义 RAG、skill 自我迭代和多轮运行，这些目录会很快变得必要。phase 1 可以先轻量预留，不必一开始填满。

### 6. Skill 的“定义资产”和“执行代码”需要分开

README 中提到“定义明确的 skill 定义”和“可自我迭代的 skill 设置”。这通常意味着 skill 不只是 Python 代码，也可能包括：

- prompt / instruction
- metadata
- input / output schema
- evaluator 配置
- execution policy
- few-shot examples
- RAG 绑定信息

因此建议区分：

```text
skills/                  # 可编辑的 skill 定义资产
src/icu_mujica/skills/   # 加载、校验、注册、运行 skill 的代码
```

这样后续 skill 自我迭代时，可以主要修改 `skills/` 下的定义资产，而不是直接改核心 runtime 代码。

### 7. `docker_compose.yml` 建议改为标准命名

建议使用下面任一标准文件名：

```text
compose.yml
docker-compose.yml
```

`docker_compose.yml` 并非不能使用，但不是 Docker Compose 默认优先识别的常见命名。

### 8. `.env` 不应作为计划提交文件

建议改成：

```text
.env.example
```

真实 `.env` 应加入 `.gitignore`。后续如果涉及 LLM API key、RAG 数据库路径、远端服务凭据，这一点尤其重要。

### 9. `requirements.txt` 可以先用，但中长期建议考虑 `pyproject.toml`

如果 phase 1 只做快速实验，`requirements.txt` 足够。

但如果项目会形成包结构、测试、lint、类型检查、命令行入口，建议尽早迁移或补充：

```text
pyproject.toml
```

可用于统一管理：

- 项目元信息
- 依赖
- dev dependencies
- pytest 配置
- ruff / mypy 配置
- console scripts

## 建议的修改路线

### Phase 1A：先明确概念边界

目标：不急着创建大量目录，先把核心概念定义清楚。

建议确认以下问题：

1. Skill 是什么？
   - 是 prompt + schema + metadata？
   - 是 Python class / function？
   - 还是两者组合？

2. Workflow 是什么？
   - 是纯 Python 编排代码？
   - 是 YAML / JSON / Markdown 形式的可配置流程？
   - 是否需要动态规划或 SkillGraph？

3. Evaluate / Execute 是什么？
   - 是每轮 skill 调用的固定协议？
   - 是 workflow 中的两个阶段？
   - 是独立的 skill 类型？

4. Harness 负责什么？
   - 会话状态？
   - 多轮对话？
   - 调用 LLM？
   - 调用工具？
   - 管理 workspace 和 artifacts？

建议输出一个短文档，例如：

```text
docs/architecture.md
```

用于记录这些边界。

### Phase 1B：采用较稳妥的轻量目录结构

建议先使用如下结构：

```text
ICU-MUJICA/
|- src/
|  |- icu_mujica/
|  |  |- harness/          # 主调度、会话、多轮运行
|  |  |- workflows/        # 原子工作流与编排逻辑
|  |  |- skills/           # skill 加载、注册、校验、运行代码
|  |  |- schemas/          # Pydantic 类型、协议模型
|  |  |- rag/              # RAG 检索、索引、数据维护
|  |  |- runtime/          # 执行上下文、状态、artifact 管理
|  |  |- utils/            # 小型通用工具
|- skills/                 # skill 定义资产，prompt / yaml / metadata
|- workflows/              # 可配置 workflow 定义，如需要
|- docs/                   # 架构说明与设计记录
|- examples/               # 最小 demo 与 golden cases
|- tests/                  # 单元测试与集成测试
|- workspace/              # 本地运行工作区，通常 gitignore
|- artifacts/              # 输出产物，通常 gitignore
|- pyproject.toml
|- compose.yml
|- .env.example
|- README.md
```

这版结构的重点是分清：

- `src/icu_mujica/`：核心 Python 包。
- `skills/`：skill 定义资产。
- `workflows/`：可配置流程资产。
- `workspace/`：运行中间态。
- `artifacts/`：运行输出结果。

### Phase 1C：如果想更小步，可以先只做最小工程骨架

如果当前还处在非常早期的探索阶段，可以先不引入太多目录，只保留最关键边界：

```text
ICU-MUJICA/
|- src/
|  |- icu_mujica/
|  |  |- harness/
|  |  |- workflows/
|  |  |- skills/
|  |  |- schemas/
|- skills/
|- tests/
|- workspace/
|- artifacts/
|- pyproject.toml
|- .env.example
|- README.md
```

这个版本更适合快速验证：

- skill 定义是否能被加载。
- schema 是否能校验输入输出。
- harness 是否能跑通一轮 evaluate + execute。
- artifacts 是否能稳定落盘。

### Phase 1D：用一个最小闭环验证目录是否合理

目录结构不要只靠想象定稿。建议 phase 1 通过一个最小闭环验证：

```text
用户需求
 -> harness 创建 session
 -> workflow 选择 skill
 -> skill 读取定义资产
 -> schema 校验输入
 -> evaluate 生成执行计划或检查项
 -> execute 产生结果
 -> runtime 写入 artifacts
 -> harness 汇总输出
```

只要这个闭环能跑通，就能反过来验证：

- `schemas/` 是否够用。
- `skills/` 定义资产是否放对了。
- `src/icu_mujica/skills/` 运行代码是否清晰。
- `workflows/` 是否真的需要配置资产目录。
- `runtime/` 是否能统一管理状态与输出。

## 推荐的短期任务清单

1. 在 README 中把 v2 结构从“计划构成”调整为“phase 1 proposal”。
2. 新增 `.env.example`，并在未来加入 `.gitignore` 忽略真实 `.env`。
3. 决定使用 `compose.yml` 或暂时不引入 Docker Compose。
4. 明确 `skill` 的组成：definition asset + runtime implementation。
5. 建立最小 Python 包结构 `src/icu_mujica/`。
6. 增加 `tests/`，先测试 schema 和 skill loading。
7. 用一个最小 demo 验证 evaluate + execute 闭环。

## 暂定结论

当前 README 中的目录方案不是方向错误，而是还处在概念映射到工程结构的早期版本。它已经抓住了 Harness、Skill、Workflow、Evaluate、Execute、Workspace、Output 这些关键词，但这些关键词目前还被放在同一层级里，导致职责边界不够稳定。

phase 1 最重要的不是创建更多目录，而是先确立以下分层：

```text
定义资产层：skills/、workflows/、configs/
核心代码层：src/icu_mujica/
协议模型层：schemas/、protocols/
运行状态层：runtime/、workspace/
输出产物层：artifacts/
验证样例层：tests/、examples/
```

当这几个层次稳定后，后续再接入 SkillGraph、RAG、自我迭代 skill、多轮会话和 IC flow 工具链，会比直接在 `src/skill/evaluate/execute` 下继续扩张更容易维护。
